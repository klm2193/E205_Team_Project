% tunnel diode bifurcation
clc
clear
syms x1 x2 x3

% define variables
R = 1.5e3; % Ohm
C = 2e-12; % F
L = 5e-6; % H

tpulse = 1e-7; % s
Vmax = 1; % V

tspan = 15e-8;
t_output=[0:1e-10:tspan];
Vin = Vmax * (t_out < tpulse) + 0 * (t > tpulse);

eqn1 = 